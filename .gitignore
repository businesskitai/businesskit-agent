# Environment — never commit real credentials
.env
.env.local

# Dependencies
node_modules/

# Build output
dist/
.wrangler/
*.js.map
memory/
businesskit-files/

# Personal preferences — local only, not for GitHub
memory.md

# Repo-local reference docs — for working with Claude, not for GitHub
PROJECT.md
ARCHITECTURE.md
CODEBASE.md
DOMAINS.md
CONVENTIONS.md

# context/ IS committed — it's your brand docs (no secrets)
# agent_skills table in Turso = live skills (not in repo)

# CPR session logs — local context, not for GitHub
CC-Session-Logs/.DS_Store
