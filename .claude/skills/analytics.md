# /analytics-agent — Analytics Snapshot

1. `atlas.snapshot()` → full picture: traffic, revenue, products, links, forms
2. `atlas.revenueReport()` → product revenue breakdown
3. `atlas.trafficReport()` → clicks by day, country, referrer, top links

ATLAS is read-only. Use ARIA for strategy derived from this data.
