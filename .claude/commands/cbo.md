# /cbo — CBO Revenue Report

1. `rex.revenueReport()` → total, trend, top products, by-type breakdown, strategy
2. `rex.pricingAudit()` → all product prices
3. `rex.conversionAnalysis()` → product clicks vs purchases

Handoff: update price → LEO | new product type → LEO | copy refresh → COPY
