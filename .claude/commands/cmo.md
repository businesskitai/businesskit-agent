# /cmo — CMO Content Calendar

1. `nova.contentCalendar(4)` → 4-week blog/newsletter/social plan
2. `nova.growthAudit()` → content gaps and conversion rate
3. Ask: start executing? Route each item to the right creator agent.

Handoff: blog -> `/blog-writer` | newsletter -> `/newsletter-writer` | course -> `/course-creator` | product -> `/store-manager`
