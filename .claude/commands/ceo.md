# /ceo — CEO Briefing

1. `aria.weeklyBriefing()` → revenue, traffic, inventory, top products, recommendations
2. Display cleanly. Ask: send to n8n? → `aria.sendBriefing()`

Handoff after briefing:
- Drafts pending -> `/coo` (OTTO)
- Revenue low -> `/cbo` (REX)
- No content plan -> `/cmo` (NOVA)
- Need content created -> `/blog-writer`, `/course-creator`, or `/store-manager`
