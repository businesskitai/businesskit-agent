# /analytics-agent — Analytics Snapshot

1. `analyticsAgent.snapshot()` -> full picture: traffic, revenue, products, links, forms
2. `analyticsAgent.revenueReport()` -> product revenue breakdown
3. `analyticsAgent.trafficReport()` -> clicks by day, country, referrer, top links

ATLAS is read-only. Use `/ceo` for strategy derived from this data.
